<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>2wild Fixed Code</title>
    <style>
        /* SKY BACKGROUND */
        body {
            margin: 0;
            padding: 0;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background: linear-gradient(to bottom, #87CEEB 0%, #E0F6FF 100%);
            overflow: hidden;
            font-family: 'Courier New', Courier, monospace;
        }

        /* CSS CLOUDS */
        .cloud {
            position: absolute;
            width: 200px;
            height: 60px;
            background: #fff;
            border-radius: 200px;
            z-index: 1;
            opacity: 0.8;
        }
        .cloud:after, .cloud:before {
            content: '';
            position: absolute;
            background: #fff;
            width: 100px; height: 80px;
            top: -40px; left: 10px;
            border-radius: 100px;
        }
        .cloud:after { width: 120px; height: 120px; top: -70px; right: 15px; left: auto; }
        .c1 { top: 15%; left: 10%; transform: scale(0.7); }
        .c2 { top: 40%; left: 80%; transform: scale(1.2); }

        /* MAIN WRAPPER */
        .content-box {
            position: relative;
            text-align: center;
            z-index: 10;
        }

        /* SMALL TEXTS */
        .intro-text {
            animation: disappear 0.5s forwards 5s; /* Vanish after 5 seconds */
        }

        .line {
            font-size: 1.2rem;
            color: #333;
            white-space: nowrap;
            overflow: hidden;
            border-right: 3px solid black;
            width: 0;
            margin: 0 auto 10px auto;
        }

        .line1 {
            animation: typing 2s steps(30) forwards, blink 0.7s infinite;
        }

        .line2 {
            animation: typing 2s steps(30) 2.5s forwards, blink 0.7s infinite;
        }

        /* BIG NAME AREA */
        .name-container {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            opacity: 0;
            animation: appear 1s forwards 5.5s;
        }

        .crown {
            width: 80px;
            height: 50px;
            background: gold;
            margin: 0 auto -10px auto;
            clip-path: polygon(0% 100%, 100% 100%, 100% 0%, 75% 40%, 50% 0%, 25% 40%, 0% 0%);
        }

        .big-name {
            font-size: 8rem;
            font-weight: 900;
            margin: 0;
            text-transform: uppercase;
            background: linear-gradient(90deg, #000, #0044ff, #000, #0044ff, #000);
            background-size: 200% auto;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            animation: shine 3s linear infinite;
        }

        /* KEYFRAMES */
        @keyframes typing {
            from { width: 0; }
            to { width: 100%; }
        }

        @keyframes blink {
            50% { border-color: transparent; }
        }

        @keyframes disappear {
            to { opacity: 0; visibility: hidden; }
        }

        @keyframes appear {
            to { opacity: 1; }
        }

        @keyframes shine {
            to { background-position: 200% center; }
        }
    </style>
</head>
<body>

    <div class="cloud c1"></div>
    <div class="cloud c2"></div>

    <div class="content-box">
        <div class="intro-text">
            <div class="line line1">hello welcome to my website</div>
            <div class="line line2">the owner is making the website</div>
        </div>

        <div class="name-container">
            <div class="crown"></div>
            <h1 class="big-name">2wild</h1>
        </div>
    </div>

</body>
</html>
# Prerequisites
*.d

# Compiled Object files
*.slo
*.lo
*.o
*.obj

# Precompiled Headers
*.gch
*.pch

# Linker files
*.ilk

# Debugger Files
*.pdb

# Compiled Dynamic libraries
*.so
*.dylib
*.dll

# Fortran module files
*.mod
*.smod

# Compiled Static libraries
*.lai
*.la
*.a
*.lib

# Executables
*.exe
*.out
*.app

# debug information files
*.dwo
